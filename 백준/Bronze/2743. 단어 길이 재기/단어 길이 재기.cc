#include <iostream>
#include <string>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    string inputStr;
    cin >> inputStr;
    cout << inputStr.length();

    return 0;
}